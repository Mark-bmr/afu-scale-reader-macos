# 隐私说明

AFU Scale Reader 的设计目标是本地优先、最少持久化和可完全删除。应用没有账户系统、广告、遥测、分析 SDK 或应用内网络请求。

## 处理的数据

首次配置会保存生理性别、身高、出生日期、输出格式、输出路径、随机 `store_id` 和 debug 开关。它们用于成人体成分估算、文件选择和存储归属校验。

每条测量记录会保存：

- 测量时间、体重和秤发送的原始阻抗码；
- 算法版本与模式；
- BMI、体脂、脂肪量、肌肉、水分、蛋白质、骨量、骨骼肌、皮下脂肪和内脏脂肪趋势估算。

输出不会保存完整蓝牙帧、设备名、peripheral UUID 或 RSSI。生理性别、身高和出生日期只在私有配置中，不会复制到每条测量记录。

## 文件位置与权限

默认的应用私有目录是：

```text
~/Library/Application Support/AFUScaleReader/
```

其中包含 `config.json`、本地权威镜像、当前日志和最多三个日志备份。用户选择的 Markdown 或 JSON 输出可以位于任何有写权限的位置。程序以原子方式写入，并在每次写入后把配置、镜像、日志和输出权限设为 `0600`。

程序只恢复带有相同 schema 与 `store_id` 的受管输出；它会拒绝覆盖普通非空文件或属于其他 store 的文件。

## 日志

debug 默认关闭。普通日志仅包含固定的启动、蓝牙状态、保存、去重和错误类别，不包含路径、体重、阻抗、原始帧、设备标识或底层错误文本。

明确开启 debug 后，日志可能包含输出路径、设备广播名、RSSI、体重、阻抗、不支持通知的原始十六进制帧和底层系统错误。这些信息只写入本地轮转日志。单个日志上限为 1 MiB，最多保留三个备份，所有文件权限为 `0600`。

## 网络与同步边界

运行中的应用不发起网络请求，也不会自动上传健康数据。

如果用户把 `output_path` 选择到 iCloud Drive、Dropbox、OneDrive、NAS 挂载目录或其他同步位置，对应服务可能上传或共享该文件。同步行为、加密、保留和访问控制由用户与该服务决定。若希望数据只留在 Mac，请选择不受同步的软件目录。

## 保留、卸载与删除

测量历史一直保留，直到用户删除。日志按大小自动轮转，不按日期上传或归档。

`./scripts/uninstall.sh` 只移除 App 和 LaunchAgent，故意保留配置、日志、私有镜像和用户输出。彻底删除时：

1. 从 `config.json` 查看并确认 `output_path`；
2. 删除 `~/Library/Application Support/AFUScaleReader/`；
3. 删除所确认的 Markdown 或 JSON 输出；
4. 如果输出曾进入同步服务，再按该服务的保留、回收站和版本历史规则清除副本。

## 非医疗用途

除体重外的体成分字段是消费级统计估算，不是医疗测量，不应用于诊断、治疗或用药决策。

隐私或安全漏洞请不要连同真实健康数据发布到公开 Issue；请使用仓库的 [GitHub 私密安全报告](https://github.com/Mark-bmr/afu-scale-reader-macos/security/advisories/new)。
